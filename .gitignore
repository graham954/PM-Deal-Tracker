# macOS
.DS_Store

# Local scrape source material (large, not needed in repo)
screenshots/

# Backup of the old screenshot-based dataset
deals.backup-screenshots.json

# Local-only scraper extension (references the retailer domain; kept off the public repo)
scraper-extension/
